apiVersion: v1
kind: ResourceQuota
metadata:
  name: staging-resource-quota
  namespace: apps-staging
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    pods: "40"
    services: "20"
    persistentvolumeclaims: "10"
